apiVersion: v1
kind: Service
metadata:
  name: order-query
  labels:
    app: order-query
  # annotations:
  #   cloud.google.com/app-protocols: '{"grpc":"HTTP2"}'
spec:
  type: NodePort
  ports:
    - name: grpc
      port: 8080
      targetPort: 8080
  selector:
    app: order-query
